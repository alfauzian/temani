import 'dart:html';

import 'package:flutter/material.dart';

class InputKehamilan extends StatelessWidget {
  const InputKehamilan({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Input'),
        centerTitle: true,
        backgroundColor: Colors.white,
        elevation: 0,
      ),
    );
  }
}
